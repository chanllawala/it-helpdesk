[tool.pytest.ini_options]
testpaths = ["tests"]
# Puts the backend root on sys.path so `import app` resolves under a bare
# `pytest`, matching how CI invokes it. Without this only `python -m pytest`
# works, which silently prepends the working directory.
pythonpath = ["."]
